# name: run portfolio with CICD

# on: [push]

# jobs:
#   backend :
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Build Backend Docker Image
#         run: docker build -t portfolio-backend ./backend

#       - name: Run Container
#         run: |
#           docker run -d -p 7000:7000 portfolio-backend

#       - name: Wait for Server
#         run: sleep 5

#       - name: Test API
#         run: curl http://localhost:7000


  # This will not work bcz of env var -> BACKEND_URL=http://backend
  # frontend:
  #   runs-on: ubuntu-latest
  #   needs: backend

  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4

  #     - name: Build Frontend Docker Image
  #       run: docker build -t portfolio-frontend ./frontend

  #     - name: Run Frontend Container
  #       run: |
  #         docker run -d -p 8000:8000 --name frontend portfolio-frontend

  #     - name: Wait for Frontend
  #       run: sleep 5

  #     - name: Test Frontend
  #       run: curl http://localhost:8000